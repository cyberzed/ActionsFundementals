name: Deploy stuff
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Where should we deploy?'
        type: environment
        required: true

jobs:
  test:
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - name: Output
        run: echo 'Testing...'
  load-test:
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - name: Output
        run: |
          echo 'Testing...'
          sleep 15
  production:
    if: inputs.environment == 'Production'
    needs: [test, load-test]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        steps: [1,2,3,4,5]
    steps:
      - run: echo 'Step ${{ matrix.steps }} deploying...'